import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  icon: {
    marginTop: 50,
    width: 24,
    height: 24,
  },
  back: {
        flex: 1,
    backgroundColor: '#000',
  }
});

export default styles;
